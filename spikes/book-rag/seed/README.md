# seed/ — hand-built fixture

A small hand-built structured JSON for **~1 section** (≈8–12 nodes) of the
slice, conforming to `_shared/schema.py`. Purpose: let Tracks B/C/D start
**before** Track A's real extractor lands — no hard blocking. Track A's real
output later swaps in behind the same schema.

Suggested: §7 Quotients (or Ch 1 §1) — a few definitions + a theorem + its
proof + an exercise, with `label`/`heading_path`/page fields filled and one
`proven_by` pair. Commit as `seed_nodes.json` (+ `seed_edges.json`).

_Task-zero deliverable — to be filled when the lab kicks off._

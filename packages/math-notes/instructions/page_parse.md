You are reading a photo of one page of handwritten mathematics study notes. Transcribe the page faithfully and literally — capture exactly what is written, as plain text, including the mathematics as it appears (use unicode math symbols or the rough notation as written). Do NOT clean it up, do NOT convert to LaTeX, do NOT correct mistakes, and do NOT add interpretation, labels, or commentary — interpretation happens in a later step.

Return ONLY the transcription as plain text: no JSON, no markdown fences, no preamble. If the page is unreadable or blank, return an empty response.

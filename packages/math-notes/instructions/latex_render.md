You are given the mathematical content of one page of handwritten study notes — possibly as unicode math symbols or rough LaTeX. Produce a single KaTeX-compilable LaTeX representation of the math on the page (use \(...\) / \[...\] delimiters around each expression). You MUST call the `validate_latex` tool and only finish once it returns valid=true; if it returns an error, fix the offending LaTeX and call it again. Put the validated LaTeX in `latex_source` and the number of validate_latex calls in `validation_attempts`. If the page has no real mathematical content, return an empty `latex_source`.
